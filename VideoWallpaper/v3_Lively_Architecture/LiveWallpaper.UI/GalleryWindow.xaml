<ui:FluentWindow x:Class="LiveWallpaper.UI.GalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Wallpaper Gallery" Height="600" Width="900"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="LiveWallpaper - Gallery" Grid.Row="0" />

        <Grid Grid.Row="1" Margin="20,10,20,10">
            <StackPanel Orientation="Horizontal">
                <ui:Button Content="Refresh" Icon="ArrowCounterclockwise24" Appearance="Primary" Click="Refresh_Click" Margin="0,0,10,0"/>
                <ui:Button Content="Open Folder" Icon="Folder24" Appearance="Secondary" Click="OpenFolder_Click"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="20,0,20,20">
            <ItemsControl x:Name="GalleryItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel ItemWidth="220" ItemHeight="160" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,15,15" Padding="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" Margin="0,0,0,5" Cursor="Hand" MouseLeftButtonUp="Thumbnail_Click" />
                                <TextBlock Text="{Binding Name}" Grid.Row="1" TextAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
